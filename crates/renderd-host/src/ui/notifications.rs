//! User Notifications integration scaffold.

/// User notification manager scaffold.
#[derive(Debug, Default)]
pub struct NotificationManager;

impl NotificationManager {
    /// Create a new notification manager scaffold.
    #[must_use]
    pub const fn new() -> Self {
        Self
    }
}
