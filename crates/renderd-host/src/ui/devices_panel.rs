//! Paired devices UI panel scaffold.

/// Paired devices window panel scaffold.
#[derive(Debug, Default)]
pub struct DevicesPanel;

impl DevicesPanel {
    /// Create a new devices panel scaffold.
    #[must_use]
    pub const fn new() -> Self {
        Self
    }
}
